#include<stdio.h>
#include<string.h>
/* To ask user to enter their first name and
 printback their name to them*/
 int main (){
    char fullname[100];
    // for first name
       gets(fullname);
       puts(fullname);

        return 0;
 }