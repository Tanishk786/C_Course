#include<stdio.h>
// Illustrate Arrays
int main (){
     int age[3];
     printf("Enter father's age :");
     scanf("%d", &age[0]);
      printf("Enter father's age :");
     scanf("%d", &age[1]);
      printf("Enter father's age :");
     scanf("%d", &age[2]);
    return 0;
}